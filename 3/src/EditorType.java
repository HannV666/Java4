/*
Создать перечисление, содержащее различные типа редакторов. !!!!!!
Создать метод, который принимает перечисление (???)         !!!!!!
В зависимости от переданного типа редактора(enum) создавать
объект соответсвующего интерфейса (который был создан в первой задаче).
Продемонстрировать вызов методов.
 */
public enum EditorType {
    WORD,
    NOTEPAD,
    SIMPLENOTE
}
